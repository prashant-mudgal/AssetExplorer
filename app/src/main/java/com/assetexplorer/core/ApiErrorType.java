package com.assetexplorer.core;

public enum ApiErrorType {
    INVALID_PARAMETER,
    INVALID_GRANT,
    TIMEOUT_ERROR,
    NO_CONNECTION_ERROR,
    NETWORK_ERROR,
    PARSE_ERROR,
    SERVER_ERROR,
    NOT_FOUND,
    UNKNOWN,
    VALIDATION,
    REDIRECT,
    PROXY_ENABLE
}
